#include<stdio.h>
int main()
{
int n,i,count;
printf("Enter N=");
scanf("%d",&n);
for(count=0,i=2;i<=n-1;i++)
{
if(n%i==0)
{
count++;
}
}
if(count==0)
printf("Prime number");
else
printf("Not Prime number");
return 0;
}
  